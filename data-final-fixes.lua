-- ------------------------------------------------------------------------------------------------
-- -------- 调整原版树木 --------------------------------------------------------------------------
-- ------------------------------------------------------------------------------------------------

if SIStartup.SIMODERTS.hard_trees() then
	for i , v in pairs( SIGen.GetList( SITypes.entity.tree ) ) do
		if v and v.max_health then v.max_health = v.max_health * 100 end
	end
end